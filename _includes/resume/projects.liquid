<ul class="flight-log">
  {% for content in data[1] %}
    <li class="log-entry">
      <div class="log-date">
        {% if content.startDate %}
          {{ content.startDate | date: '%Y.%m' }}<br>
          &ndash; {{ content.endDate | date: '%Y.%m' | default: 'now' }}
        {% endif %}
      </div>
      <div class="log-content">
        {% if content.url %}
          <a class="log-title" href="{{ content.url }}" target="_blank" rel="noopener noreferrer">{{ content.name }}</a>
        {% else %}
          <span class="log-title">{{ content.name }}</span>
        {% endif %}
        {% if content.summary %}<p class="log-summary">{{ content.summary }}</p>{% endif %}
        {% if content.highlights %}
          <ul class="tag-list">
            {% for item in content.highlights %}<li class="tag">{{ item }}</li>{% endfor %}
          </ul>
        {% endif %}
      </div>
    </li>
  {% endfor %}
</ul>
