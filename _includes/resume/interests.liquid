{% for content in data[1] %}
  <div class="skill-group">
    <h4 class="skill-group-title">{{ content.name }}</h4>
    <ul class="tag-list">
      {% for item in content.keywords %}<li class="tag">{{ item }}</li>{% endfor %}
    </ul>
  </div>
{% endfor %}
